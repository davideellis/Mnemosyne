[package]
name = "core_sync"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"
description = "Core sync and vault domain models for Mnemosyne"

[lib]
name = "core_sync"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

